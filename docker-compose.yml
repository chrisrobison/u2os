version: '3.9'

services:
  app:
    build: .
    depends_on:
      - postgres
    env_file:
      - config/env/development.env
    environment:
      DB_CLIENT: postgres
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: u2os
      DB_PASSWORD: u2os
      DB_NAME: u2os_tenant
      CONTROL_DB_CLIENT: postgres
      CONTROL_DB_HOST: postgres
      CONTROL_DB_PORT: 5432
      CONTROL_DB_USER: u2os
      CONTROL_DB_PASSWORD: u2os
      CONTROL_DB_NAME: u2os_control
      AUTH_JWT_SECRET: replace-me-with-at-least-32-chars
      TENANCY_BOOTSTRAP_HOST: localhost
      TENANCY_BOOTSTRAP_DOMAIN: localhost
      TENANCY_STRICT_HOST_MATCH: "true"
    ports:
      - "3010:3010"
    restart: unless-stopped

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: u2os
      POSTGRES_PASSWORD: u2os
      POSTGRES_DB: u2os_tenant
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  reverse-proxy:
    image: nginx:1.27-alpine
    profiles:
      - proxy
    depends_on:
      - app
    ports:
      - "8080:80"
    volumes:
      - ./config/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  pgdata:
