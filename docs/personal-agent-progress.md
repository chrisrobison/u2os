# Dependable personal agent progress

Tracking issue: [#172](https://github.com/chrisrobison/u2os/issues/172). This record distinguishes code, fixture evidence, and owner-account validation. Update it after each merged slice.

| Area | Implemented | Fixture tested | Live validated | Remaining |
|---|---|---|---|---|
| Connector instances | Schema, legacy migration, CRUD, OAuth state binding, provider routing, named setup UI (PRs #168–#171, #174), consequential email/calendar account binding (#177), explicit IMAP/SMTP pairing (#178), persisted per-account/per-domain sync health (#179) | Node and browser flows for separate accounts, migration, OAuth binding, approved Gmail account selection, SMTP sender routing, and isolated sync status | No | Owner-driven live Google/IMAP validation (#150) |
| Personal initialization | Durable owner-to-entity link (#182); clean personal startup and persistent isolated demo mode (#185); connector and planner mock fail-closed in personal mode (#186, #189) | Rename/restart/legacy-link tests; fresh personal, explicit demo, unmarked legacy, disconnected-service, cache-label, and planner-status fixtures | No | Review-based cleanup of ambiguous old fixtures (no automatic deletion); live provider validation is owner-driven |
| Agent runs | Policy gate and durable action queue; within-plan dependency gate (#191); durable run/step metadata and restart reconciliation (#193); model-bound observation privacy filter (#198); bounded read-result continuation with validated references (#194); deferred same-plan dependencies resume after approval (#201); persisted bounded checkpoints after approval/restart including completed consequential results (#203); durable cancellation between steps and safe unleased-queue revocation (#205); persisted step/model/elapsed limits (#207); provider-reported token cap and coverage (#209); exact-call privacy/reference/provenance metadata across concurrent planning (#244); bounded background reconciliation after delayed delivery/restart (#246) | Node and browser approval/queue suites; dependency status, transitive-skip, run/approval/restart, old-schema, observation privacy/fallback/injection, multi-step draft, approval-dependent, checkpoint, in-flight cancellation, cross-restart budget, concurrent reference/isolation/withheld-output, fallback, delayed/duplicate/idle wakes, scan fairness, stopped goals and shutdown drain; browser closure/return fixtures | No | Monetary cost when reliably available, objective verification, broader conversational progress presentation |
| Conversations and search | Structured memory and limited context retrieval; selected-account Gmail provider search and IMAP recent-cache search (#211); owner-scoped durable turns and browser transcript restore (#213); bounded destination-filtered prior turns with source IDs (#215); saved-conversation picker (#217); bounded prior read artifacts with account provenance (#219); validated prior-item references for email read, task completion, and calendar reschedule (#221) | Context/privacy, deterministic Gmail/IMAP search, conversation restart/isolation/API/browser, picker, read-artifact scope/restart/privacy/injection, prior-item approval/account-switch/failure, and history/fallback fixtures | No | Broader artifact references, summaries, user-facing coverage/freshness, owner classification controls |
| Goals | Scheduler and event triggers; owner-scoped, revision-safe goal drafts (#223), native draft UI (#225), explicit read-only linked runs with scope and cumulative budget enforcement (#228), bounded owner-only run evidence (#230), durable pause/resume/cancel controls (#232), safe paused scope revision with retained ledger and historical objective labels (#234), persisted owner-selected one-time wake with atomic run consumption (#236), deduplicated successful search findings with provenance and explicit owner review (#238), bounded destination-filtered exact-goal prior read context (#240), deterministic per-run new/repeated research updates (#242), source-bound destination-filtered owner reviews with inherited sensitive provenance (#248); editable job research starter/two-pass walkthrough (#250), finite owner-selected web research backend/UI (#252, #254); no completion claim | Scheduler; draft migration/restart/owner/API/browser; linked-run scope/queue/budget/failure/migration/API/browser; durable evidence/privacy/truncation/XSS; lifecycle/revision/in-flight cancellation, changed-scope/budget, wake concurrency/restart/outage; finding dedup/review/restart/migration/limits/unsafe-URL/storage failure; goal context restart/isolation/scope/fallback/continuation/nested privacy/reference; research delta/partial coverage/projection failure/API/browser; owner review isolation/restart/revision/count/total/visibility limits, mixed-sensitive-source and safe old-schema migration fixtures; two-pass job/relevance/reload/review, finite schedule concurrency/restart/blocker/budget and native schedule fixtures | No | Owner-driven fit/current-opening quality validation, broader review retrieval/classification controls |
| Operations | Device trust/export/action diagnostics; deterministic queue timing (#152); development-only raw device debug routes (#256); demo-only mock adapters (#258); account-scoped sync drain (#260); bind-safe workers (#262); canonical-home runtime ownership and request drain (#264); safe HTTP log metadata (#265); offline mutation ownership (#270); coherent private staged SQLite snapshot creation (#272); opt-in authenticated archive encryption (#274); validated isolated inactive restore (#276), stable installation identity (#278), stopped-work/connectivity quarantine (#280, #284), guarded read-only post-capture comparison (#286), bounded real Brave read failures (#288) | Node/browser boundaries and failure paths; deterministic heartbeat, overlap/drain, startup rejection, process interruption, log redaction, offline CLI preservation, WAL/row-ID/old-schema/credential snapshot round trips, corrupt/link/alias/overwrite/collision failures; streamed encryption, tamper/truncation/secret/target-preservation and masked-input fixtures; strict archive metadata/path/type/limit checks, SQLite integrity, no-clobber publication and SIGKILL-before-completion recovery refusal; quarantine preservation/interruption, dual-home WAL evidence/counts/privacy, stalled native search body and sanitized errors | No | Explicit activation/original-instance retirement and stale/uncertain-action reconciliation; broader outage recovery |
| Daily workflows | Personal API brief/draft/meeting/outage on fresh and existing homes (#295); demo research and finite schedule; [owner acceptance procedure and private two-week scorecard](personal-acceptance.md) | Eight personal-mode cases plus component/demo suites; not a consolidated six-workflow proof | No | Remaining personal research/approval/interruption/browser harness, live owner walkthrough and two-week quality evidence |

Owner action required for [#150](https://github.com/chrisrobison/u2os/issues/150): connect a real Google account and perform an opt-in Calendar/Gmail smoke check. Automated tests use isolated fixtures and have not accessed personal accounts, sent real messages, or changed real events.

Job research workflow (#250): an unsaved web-only starter and [owner walkthrough](job-research-walkthrough.md) exercise two bounded fixture passes with evidence-based fit/mismatch explanations, retained owner review, new/repeated indexed links, reload, cumulative spending and pause. Finite research (#252) adds an owner/revision-bound web-only API, daily-or-slower pass limits, persistent scheduler checkpoints and status view; native creation (#254) adds explicit start/interval/count inputs with saved-scope and remaining-budget notices. Fixtures cover idle/concurrent/restarted ticks, no catch-up bursts, failed/uncertain/no-read outcomes, cumulative budgets, rollback, migration, bounded scan fairness, auth/CSRF, browser creation/reload/lifecycle, invalid/unsaved input, stale revisions and unavailable scopes/budgets. These are deterministic orchestration tests, not live search validation or proof of real-model relevance quality. Operational hardening and owner-driven quality validation remain.

Deferred: native mobile/watch apps, broad connector catalog, hosted service, multi-device sync, and a coding runner. A coding runner needs its own scoped design with isolated workspace, filesystem and resource boundaries, Git, tests, and artifact handling.

HTTP log privacy (#265): access logs use registered route templates/fixed labels instead of raw URLs or private path parameters; router errors retain safe status metadata without raw exceptions. Pretty/JSON HTTP fixtures cover OAuth code/state, arbitrary queries/fragments, encoded/malformed paths, static/unmatched requests and unknown methods; an exception fixture verifies private provider text is omitted. Existing owner/platform logs were neither read nor scrubbed. No live OAuth validation was performed.

Runtime ownership (#264): a dedicated local SQLite exclusive guard is acquired before application migration/reconciliation, with canonical-home alias handling and no lease expiry. Started HTTP handlers and background work drain before release; disconnected clients do not permit premature replacement. An idempotent shutdown method and observable closed promise support isolated fixtures. Duplicate-process, interruption/uncertain-action, failed bootstrap, alias/independent-home, fresh/demo/legacy and unknown-file/symlink fixtures exercise the boundary. Only successfully owned guard artifacts are excluded from fresh-demo emptiness checks. Existing records and queue semantics remain intact. Maintenance/backup coordination, cross-host/restored-copy safety and useful provider deadlines remain unfinished; no live validation was performed.

Startup bind safeguard (#262): queue, sync and trigger workers start only after successful HTTP bind; bind rejection cleans prepared device adapters. Isolated occupied-port fixtures preserve queued rows and zero attempts in fresh/configured personal homes, clear heartbeat resources and leave another running home's workers intact; successful-bind/close fixtures retain configured timers. Worker-setup failure after bind closes the listener and drains partial setup. An idempotent internal background drain does not close HTTP or cancel pending request handlers. Local initialization can still precede binding. Cross-process singleton/maintenance locking, complete runtime shutdown and safe backup/restore remain unfinished.

Sync lifecycle (#260): same-home/domain/provider/account polling and manual calls share one in-flight operation, including sanitized failures. Other accounts/domains/homes stay independent; reconciliation preserves work without catch-up retries. An awaited timer shutdown drains already-started operations without claiming cancellation. Deterministic entry/release and captured-timer fixtures cover overlap, switches, failures, retries, stale callbacks and success/failure drain. This is a recovery prerequisite, not yet a complete runtime/maintenance lock or safe live backup; no live sync validation was performed.

Device execution boundary (#256): raw invoke/test/stream-open routes deny before adapter access by default and in production; only explicit non-production development opt-in enables them. Native controls fail closed on missing debug metadata. Fixtures cover not-attempted denials, forged flags, production override, session/CSRF, revocation, unchanged policy-gated presentation, metadata/discovery, and browser disabled/development/unknown states. Personal mock-device separation (#258) confines mock adapters to explicit demo homes; cached legacy mock and real records preserve owner edits/trust but cannot resolve without their adapter. APIs and native views distinguish demo provenance, adapter registration and last observation. Fresh, upgraded, restarted, failed-real and demo fixtures cover the boundary; no live hardware validation was performed. Consistent encrypted backup/restore and broader outage recovery remain.

Notification account identity (#267): proposals now capture the exact provider/instance/credential revision before approval or enqueue, and execution uses that binding. Isolated fixtures cover approval and queued account switches, SQLite reopen, deleted/disconnected/reconfigured accounts, legacy missing bindings, changed payloads and uncertain interrupted delivery without replay. Browser approval probes identify the original named account without webhook URLs. No real owner webhook delivery or live account validation performed.

Offline mutation boundary (#270): setup-owner, seed and maintenance CLI entry points acquire the canonical-home guard before application storage/config access. Subprocess fixtures cover unopened/active/aliased homes, unchanged records/credentials, fresh maintenance, retained records, asynchronous failure drain, successful owner setup/refusal, and explicit idempotent demo versus denied personal seeding. Stop the runtime before these commands; imported store helpers, external editors and older releases are outside this cooperative boundary. Backup/restore consistency, encryption and isolated inactive recovery remain unfinished. No owner data was maintained or backed up.

Coherent offline snapshot creation (#272): backup acquires the same guard before application reads and holds through private staging/atomic no-clobber publication outside the source home. SQLite backup captures committed WAL and preserves row IDs/uncertain attempts without migrating the source; staged integrity checking and self-contained database mode exclude raw WAL/SHM and runtime locks. Fixtures cover retained old schemas, credential/config round trips, source preservation, ownership through await, alias/overwrite/collision denial, corrupt storage and unsupported links with failure cleanup. Node 22.16+ is needed for SQLite backup. Current archives are still outer-unencrypted; legacy restore lacks safe inactive-copy ownership and must only be tested in isolation. Encryption and recovery remain subsequent work. No owner backups were created or restored.

Authenticated archive encryption (#274): opt-in streamed AES-256-GCM with an independent passphrase, random salt/nonce and fixed bounded scrypt; header and payload authenticate before extraction. Fixture coverage includes SQLite/config/credential round trips, multi-megabyte streaming, wrong secrets, header/cipher/tag tampering, truncation, target/source preservation, plaintext expectation denial, unique salts/nonces, no-clobber, environment/argument/log boundaries and masked prompt cancellation/EOF/confirmation cleanup. Legacy plaintext creation remains explicitly labeled unencrypted. [Backup guidance](backups.md) distinguishes encryption from unfinished safe archive entry validation, inactive isolated recovery and original/restored executor coordination. No owner secrets or backups were used.

Validated inactive recovery (#276): strict streaming tar/gzip validation replaces direct extraction; bounded PAX/GNU names are validated and macOS binary attributes discarded. Reserved authority files/sidecars, unsafe paths, links/special files, duplicates, corruption and limits fail before destination publication. Read-only SQLite integrity checks preserve old schemas/row IDs without migrations. Only an empty canonical home may publish under ownership; force-merging is removed. A durable incomplete marker precedes payload writes and successful verification remains inactive. Runtime/offline CLI startup refuses every recovery marker before migrations, reconciliation, providers or workers. Fixtures include encrypted credentials, retained uncertain attempts, publication failure and a deterministic SIGKILL after the first file with refusal in a fresh process. Current Node suite: 699 tests; browser suite: 255 tests. [Offline verification](backups.md) is supported; explicit activation, original-instance retirement and stale-action reconciliation are not yet supported. No owner backups, accounts or secrets used.

Stable installation identity (#278): fresh personal/demo homes receive distinct random canonical UUIDv4 IDs in existing installation metadata, separate from authenticated owner/entity/account identity. Additive upgrade preserves unknown JSON fields verbatim (including large integers/escapes/formatting), creation time, mode and SQLite records; repeated initialization does not rewrite metadata. Private atomic/fsynced persistence and sanitized malformed/oversized/unsafe-file refusal run before application startup. Read-only backup sources stay unchanged; inactive restored markers record known identity or honest legacy absence. Fixtures cover rename/restart/owner-name changes, upgrade/failure preservation, late persistence failure without identity regeneration, symlink/hardlink denial, invalid identity/runtime refusal and known/legacy backup round trips. IDs are not authentication or cross-path locks; activation/retirement and stale-action reconciliation remain separate work. Node suite: 715 tests; browser suite: 255 tests. No owner account/data validation performed.

Offline database-work quarantine (#280): preview returns counts only on verified inactive identity-matched recovery homes, without migrations or providers/models. Explicit apply transactionally revokes archived authorization, holds unfinished queues for owner review, cancels run continuation, pauses/revisions goals, stops triggers/wakes/finite schedules and revokes archived sessions. Completed results, attempt history, personal records and cumulative resource counters remain. Per-restore IDs and stopped-state validation make checkpoints idempotent; rollback, publication failure and deterministic SIGKILL/retry fixtures keep the execution barrier intact. Linked storage and unsupported/executable schemas (including custom expression indexes) are refused. Run/operation metadata labels restored snapshot uncertainty without inventing definite failure or non-attempt; old approvals cannot retry. [Preview/apply](backups.md#preview-and-quarantine-unfinished-database-work) covers database work only: connectivity review, original retirement and activation remain separate prerequisites. Node suite: 729 tests; browser suite: 258 tests. All evidence is fixtures, not owner-account validation.

Account eligibility (#282): retained encrypted credentials alone cannot enable pending, disconnected, error or unknown-status accounts. The shared connected/live/service-credential check governs provider selection, bound execution, proposals, sync, health and Google service flags. Explicit held selection never borrows another account; rename/disconnect cannot reconnect a held account. Explicit validated credential update advances status and revision; Google uses the existing OAuth completion. Node fixtures cover all real domains, IMAP, alternate accounts and API transitions; browser fixtures show held Google services as disconnected with reconnect controls and no credential content. Recovery connectivity quarantine and activation remain unfinished; no live accounts accessed.

Offline connectivity quarantine (#284): guarded counts-only preview follows a validated stopped-work checkpoint. Explicit apply retains archived encrypted/legacy credentials, device token and whole runtime model/connector config byte-for-byte in a private per-restore review area, leaving the master key and personal records intact. Live accounts disconnect with revision invalidation and fresh SMTP association required; cached devices revoke/offline. A bounded inventory, verified durable staged copies and transactional metadata-only audit support interrupted-copy/transaction/final-marker retry without stale permission reuse. New backups suppress synthesized macOS AppleDouble sidecars; legacy sidecar bytes remain privately preserved. Fixtures cover source/archive preservation, loader/migration/model unavailability, owner/attempt/resource retention, links/limits/unknown files/invalid schemas, changed bytes, rollback, publication failure, inventory tampering, CLI redaction and deterministic SIGKILL at file and database boundaries. Connectivity preparation is not activation; original retirement and post-capture reconciliation remain unfinished. Node suite: 759 tests; browser suite: 261 tests. No live owner data, model/provider calls or device connections.

Post-capture evidence comparison (#286): an explicitly selected existing original and prepared inactive recovery must both be guarded and match known personal installation/stable owner-entity identity. A private SQLite-only consistent copy includes original committed WAL progress without migrations or credential/config/record mutation. Metadata-only output reports action/attempt/run/goal drift, recorded usage beyond capture, unmetered calls/regressed counters, and owner-auth/policy-byte changes. It never prints private content, IDs, paths, credentials or fingerprints. Fixtures cover WAL/source/target preservation, staging cleanup, owner rename, new/missing goals, unknown usage, both guard-failure orders, aliases/missing/mismatched identity, changed quarantine, linked/unsafe storage and schemas, and CLI boundaries. No delivery verification, ledger reconciliation, retirement or activation is authorized; the original can progress after inspection. Node suite: 774 tests; browser suite: 261 tests. No live owner data/providers/models were used.

Bounded real search (#288): Brave GET/header and JSON-body reads share a 10-second default deadline, abort/late-result discard and timer/body cleanup. Sanitized timeout, authorization, rate-limit and unavailable errors expose no query, credentials or upstream text; error-code impersonation cannot bypass sanitization. There is no automatic retry or mock substitution, and exact selected-account credentials/success shape are unchanged. Deterministic fixtures and an isolated stalled native-HTTP body cover success, missing accounts, non-cooperating/late transports, parsing, status failures, cleanup and invalid deadline overrides. Google reads/OAuth and consequential delivery remain separately scoped. Node suite: 788 tests. No live owner/provider validation.

Owner acceptance procedure (#290): [six-workflow walkthrough and private two-week scorecard](personal-acceptance.md), explicit fresh/upgrade data preservation and read-only owner-driven live boundaries, honest component/demo evidence links and deferred consolidated personal-mode acceptance. Overview docs now reflect bounded scheduled goals and no personal mock fallback. The separately isolated coding runner is deferred in #291; no shell tool is added. This is documentation, not a completed live walkthrough or dogfooding record. Local evidence links verified; documented Node subset: 93 passing tests; full Node suite: 788 tests; browser suite: 261 tests.

Goal auxiliary-model boundary (#293): persisted goal linkage disables optional embeddings in a request-local lexical context assembler, preserving owner identity, limits, provenance and actual-destination privacy filtering without shared-provider mutation. Both candidate/fact ranking skip unreserved auxiliary requests and cache writes; ordinary chat/standalone semantic retrieval remains configured. Fixtures cover useful context, concurrency, sensitive withholding, cumulative budget after restart and competed persisted continuation claims without read replay. Ordinary-chat embedding usage remains outside the planning ledger; metering must precede future goal embedding support. Full Node suite: 795 tests. No live models/providers or owner data were used.

Personal draft identity (#296): assessment for personal acceptance #295 found shared local drafting assigned the demo owner's address. New personal/unmarked-legacy drafts now explicitly leave sender empty; owner display names, account configuration and disconnection are not used to fabricate identity. Demo drafts retain their fictional sender. Existing drafts/contact/owner records are preserved; drafting creates no provider call, send event or queued delivery, and later send account binding/approval is unchanged. Fixtures cover direct store/tool/offline use, rename/stable owner link, demo separation, restart preservation and invalid-mode refusal. Consolidated personal acceptance #295 resumes after this prerequisite. Full Node suite: 802 tests. No live owner/provider data used.

Personal workflow acceptance (#295): `npm run test:personal` runs eight fixed-clock API cases for brief, grounded draft, event-selected meeting preparation and failed-provider continuation on fresh and existing unmarked homes. Actual personal startup, owner authentication, encrypted selected accounts, Google adapters and configured compatible-model HTTP use isolated scripted responses; older-first mail requires observed timestamps and validated references. Restart retains results without further reads/model calls; renamed owner/prior contact/mail survive upgrade. Unknown attendees create no invented contacts, drafts have no fictional sender, and outage overrides a false completion claim without drafting. No provider writes, live accounts or real models are used. This is the first partial consolidated personal acceptance slice, not all six workflows, personal browser/OAuth proof, every historic schema or real-model quality validation. Full Node suite: 810 tests; browser suite: 261 tests.

Google refresh race boundary (#299): a refreshed token is discarded before synchronous persistence/return if the service-token snapshot or effective shared/legacy-local OAuth client changed while awaiting the provider. Disconnect/removal cannot recreate tokens/vault files; replacement and the first completed concurrent refresh remain byte-for-byte intact. Other services/accounts are preserved. Eight delayed-response fixtures plus existing OAuth coverage verify sanitized actionable conflicts and compatible success; account eligibility/approval remains an independent runtime boundary. This is not a network deadline, permission lease or automatic retry. Full Node suite: 818 tests; browser suite: 261 tests. No live accounts or provider calls used.
