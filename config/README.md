Configuration is environment-driven via `.env` and `core/config.js`.

Primary settings:
- `PORT`
- `DB_CLIENT` (`mysql`, `postgres`, or `sqlite`)
- `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME` (for MySQL/Postgres)
- `DB_FILE` (for SQLite)
- `MODULES_DIR`
